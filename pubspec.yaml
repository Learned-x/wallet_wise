name: wallet_wise
description: Wallet Wise - Personal Finance AI App (scaffold)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  flutter_riverpod: ^2.0.0
  drift: ^2.5.0
  path_provider: ^2.0.0
  sqlite3_flutter_libs: ^0.5.0
  sqflite_sqlcipher: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.0
  drift_dev: ^2.5.0

flutter:
  uses-material-design: true
